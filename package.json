{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy:admin-ui": "yarn workspace cms-admin-ui build && yarn workspace cms-admin-ui migrate"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
